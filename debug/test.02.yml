base_task:
  input: cat ~/xxdr.pl
  transform: cat -n | awk '{print $1}'
  preview: echo "{}"
  opts:
  - reverse
  - multi
  output:
      enter:
      - pipe: cat
switch_task:
  ctrl-r:
    input: echo "{pre_content}"
    transform: ''
    opts:
    - multi
    bind:
      alt-p: select-all
    output:
      enter:
      - pipe: sed 's/^/XXXX/'

