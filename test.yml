init_task:
  input: git log --graph --decorate --oneline --color=always
  opts:
    - exact
    - multi
    - no-mouse
    - ansi
  preview: echo preview={}
expects:
  ctrl-t:
    continue: true
    input: date
    query: '1'
    preview: pwd

