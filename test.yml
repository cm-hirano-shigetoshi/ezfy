init_task:
  input: git log --graph --decorate --oneline --color=always
  opts:
    - exact
    - multi
    - no-mouse
    - ansi
  preview: echo preview={}
expects:
  ctrl-t:
    stdout: cat -n | pbcopy
    continue:
      input: date
      query: '1'
      preview: pwd

